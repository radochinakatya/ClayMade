import styled from "styled-components";

const Maintitle = styled.div`
  font-size: 36px;
  font-weight: normal;
  text-align: center;
  margin-top: 60px;
  margin-bottom: 60px;
`;

const ImgOnTop = styled.div`
  text-align: center;
  margin-left: auto;
  margin-right: auto;
  filter: brightness(80%);
`;

export const S = {
    Maintitle,
    ImgOnTop
}